# R is Case Sensitive
#To assign a variable we can either use '=' or '<-', latter is preffered
#To write two statements in the same line we can use ';'
#To Run a statement we use ctrl+enter
#Variable Names cant begin with _ or number(can start with '.'), special characers beside these 2 arent allowed

a=10
b=20

cat(a,"\n",b) #Cat concatenates every argument given to it